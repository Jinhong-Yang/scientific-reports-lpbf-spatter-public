All final numerical statements were generated from aggregate evidence files after W17 completion. The release manifest records each included file's SHA-256 digest. Licensed source pixels, model checkpoints, predictions, local absolute paths, and secrets are excluded. The N2 test-access ledger records one pre-unlock display of historical specimen identifiers from a combined group-view manifest; no held-out pixels, coordinates, predictions, metrics, or outcomes were accessed, and all development folds used separate train and validation manifests.
